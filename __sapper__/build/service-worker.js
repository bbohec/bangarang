(function () {
	'use strict';

	// This file is generated by Sapper — do not edit it!
	const timestamp = 1621419697159;

	const files = [
		"/service-worker-index.html",
		"/favicon.png",
		"/global.css",
		"/logo-192.png",
		"/logo-512.png",
		"/manifest.json"
	];

	const shell = [
		"/client/client.30d5b469.js",
		"/client/index.80cb6298.js",
		"/client/MainMenu.84908b18.js",
		"/client/messages.1cb1ff8b.js",
		"/client/WelcomeTitle.7438cb37.js",
		"/client/NavigateToBusinessModel.ca34beca.js",
		"/client/GenericTaskNotification.5b1a77b9.js",
		"/client/GenericInteractiveAction.398b296b.js",
		"/client/LanguageSelect.6483682e.js",
		"/client/MainTitle.9ee2c278.js",
		"/client/Link.124a6950.js",
		"/client/index.47bc0dd9.js",
		"/client/[valuePropositionPageLink].947fdb2a.js",
		"/client/retreiveValuePropositionFromValuePropositionPageLink.de4caed6.js",
		"/client/HeaderTitle.6cd3506c.js",
		"/client/NavigateToLeanCanvas.9706ad77.js",
		"/client/NavigateToMainMenu.ce88f1c7.js",
		"/client/BusinessModel.9cab6456.js",
		"/client/DescriptionCard.bfb539a0.js",
		"/client/NavigateBackToMainMenu.d354a310.js",
		"/client/SigningInMenu.5cd2c2e2.js",
		"/client/currentClaimIdStore.5d37739c.js",
		"/client/GenericPasswordField.b7e71601.js",
		"/client/GenericSubmitField.aa990aea.js",
		"/client/DeclareClaim.28ab06c4.js",
		"/client/DeclaringInformation.c3f65b1d.js",
		"/client/[landingPageId].62df4867.js",
		"/client/GenericButton.4fd8ca15.js",
		"/client/LeanCanvas.54bb4bd4.js",
		"/client/Register.44c5ebb3.js",
		"/client/[claimId].836449e6.js"
	];

	const ASSETS = `cache${timestamp}`;
	// `shell` is an array of all the files generated by the bundler,
	// `files` is an array of everything in the `static` directory
	const to_cache = shell.concat(files);
	const staticAssets = new Set(to_cache);
	// @ts-ignore
	self.addEventListener('install', (event) => {
	    event.waitUntil(caches
	        .open(ASSETS)
	        .then(cache => cache.addAll(to_cache))
	        .then(() => {
	        self.skipWaiting();
	    }));
	});
	// @ts-ignore
	self.addEventListener('activate', (event) => {
	    event.waitUntil(caches.keys().then(async (keys) => {
	        // delete old caches
	        for (const key of keys) {
	            if (key !== ASSETS)
	                await caches.delete(key);
	        }
	        self.clients.claim();
	    }));
	});
	/**
	 * Fetch the asset from the network and store it in the cache.
	 * Fall back to the cache if the user is offline.
	 */
	async function fetchAndCache(request) {
	    const cache = await caches.open(`offline${timestamp}`);
	    try {
	        const response = await fetch(request);
	        cache.put(request, response.clone());
	        return response;
	    }
	    catch (err) {
	        const response = await cache.match(request);
	        if (response)
	            return response;
	        throw err;
	    }
	}
	// @ts-ignore
	self.addEventListener('fetch', (event) => {
	    if (event.request.method !== 'GET' || event.request.headers.has('range'))
	        return;
	    const url = new URL(event.request.url);
	    // don't try to handle e.g. data: URIs
	    const isHttp = url.protocol.startsWith('http');
	    const isDevServerRequest = url.hostname === self.location.hostname && url.port !== self.location.port;
	    const isStaticAsset = url.host === self.location.host && staticAssets.has(url.pathname);
	    const skipBecauseUncached = event.request.cache === 'only-if-cached' && !isStaticAsset;
	    if (isHttp && !isDevServerRequest && !skipBecauseUncached) {
	        event.respondWith((async () => {
	            // always serve static files and bundler-generated assets from cache.
	            // if your application has other URLs with data that will never change,
	            // set this variable to true for them and they will only be fetched once.
	            const cachedAsset = isStaticAsset && await caches.match(event.request);
	            // for pages, you might want to serve a shell `service-worker-index.html` file,
	            // which Sapper has generated for you. It's not right for every
	            // app, but if it's right for yours then uncomment this section
	            /*
	            if (!cachedAsset && url.origin === self.origin && routes.find(route => route.pattern.test(url.pathname))) {
	                return caches.match('/service-worker-index.html');
	            }
	            */
	            return cachedAsset || fetchAndCache(event.request);
	        })());
	    }
	});

}());
